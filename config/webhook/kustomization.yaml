apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- service.yaml
- deployment.yaml
- webhook-config.yaml
- certificates.yaml
- rbac.yaml
- config.yaml
- network-policy.yaml
- pdb.yaml

namePrefix: optipod-webhook-

commonLabels:
  app.kubernetes.io/name: optipod-webhook
  app.kubernetes.io/component: webhook
  app.kubernetes.io/part-of: optipod
