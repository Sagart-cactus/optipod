---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: optimizationpolicies.optipod.optipod.io
spec:
  group: optipod.optipod.io
  names:
    kind: OptimizationPolicy
    listKind: OptimizationPolicyList
    plural: optimizationpolicies
    shortNames:
    - optpol
    singular: optimizationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .spec.metricsConfig.provider
      name: Provider
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OptimizationPolicy is the Schema for the optimizationpolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OptimizationPolicy
            properties:
              metricsConfig:
                description: MetricsConfig defines how metrics are collected and processed
                properties:
                  percentile:
                    default: P90
                    description: Percentile defines which percentile to use for recommendations
                    enum:
                    - P50
                    - P90
                    - P99
                    type: string
                  provider:
                    description: Provider specifies the metrics backend (e.g., "prometheus",
                      "metrics-server")
                    enum:
                    - prometheus
                    - metrics-server
                    - custom
                    type: string
                  rollingWindow:
                    default: 24h
                    description: RollingWindow defines the time period over which
                      metrics are aggregated
                    type: string
                  safetyFactor:
                    default: 1.2
                    description: |-
                      SafetyFactor is a multiplier applied to the selected percentile
                      Must be >= 1.0. Defaults to 1.2 if not specified.
                    type: number
                required:
                - provider
                type: object
              mode:
                allOf:
                - enum:
                  - Auto
                  - Recommend
                  - Disabled
                - enum:
                  - Auto
                  - Recommend
                  - Disabled
                description: Mode defines the operational behavior of the policy
                type: string
              reconciliationInterval:
                default: 5m
                description: ReconciliationInterval defines how often the policy is
                  evaluated
                type: string
              resourceBounds:
                description: ResourceBounds defines min/max constraints for resource
                  recommendations
                properties:
                  cpu:
                    description: CPU defines CPU resource bounds
                    properties:
                      max:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Max is the maximum allowed value
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      min:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Min is the minimum allowed value
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - max
                    - min
                    type: object
                  memory:
                    description: Memory defines memory resource bounds
                    properties:
                      max:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Max is the maximum allowed value
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      min:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Min is the minimum allowed value
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - max
                    - min
                    type: object
                required:
                - cpu
                - memory
                type: object
              selector:
                description: Selector defines which workloads this policy applies
                  to
                properties:
                  namespaceSelector:
                    description: NamespaceSelector selects namespaces by labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namespaces:
                    description: Namespaces defines allow/deny lists for namespace
                      filtering
                    properties:
                      allow:
                        description: Allow is a list of namespaces to include
                        items:
                          type: string
                        type: array
                      deny:
                        description: Deny is a list of namespaces to exclude (takes
                          precedence over Allow)
                        items:
                          type: string
                        type: array
                    type: object
                  workloadSelector:
                    description: WorkloadSelector selects workloads by labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  workloadTypes:
                    description: WorkloadTypes defines include/exclude filters for
                      workload types
                    properties:
                      exclude:
                        description: Exclude specifies workload types to exclude (takes
                          precedence over Include)
                        items:
                          description: WorkloadType represents supported Kubernetes
                            workload types
                          enum:
                          - Deployment
                          - StatefulSet
                          - DaemonSet
                          type: string
                        type: array
                      include:
                        description: Include specifies workload types to include (if
                          empty, includes all)
                        items:
                          description: WorkloadType represents supported Kubernetes
                            workload types
                          enum:
                          - Deployment
                          - StatefulSet
                          - DaemonSet
                          type: string
                        type: array
                    type: object
                type: object
              updateStrategy:
                description: UpdateStrategy defines how resource updates are applied
                properties:
                  allowInPlaceResize:
                    default: true
                    description: AllowInPlaceResize enables in-place pod resize when
                      supported
                    type: boolean
                  allowRecreate:
                    default: false
                    description: AllowRecreate enables pod recreation when in-place
                      resize is not available
                    type: boolean
                  limitConfig:
                    description: LimitConfig defines how resource limits are calculated
                      from recommendations
                    properties:
                      cpuLimitMultiplier:
                        default: 1
                        description: |-
                          CPULimitMultiplier is the multiplier applied to CPU recommendation to calculate limit
                          Default: 1.0 (limit equals recommendation)
                          Example: 1.5 means limit = recommendation * 1.5
                        maximum: 10
                        minimum: 1
                        type: number
                      memoryLimitMultiplier:
                        default: 1.1
                        description: |-
                          MemoryLimitMultiplier is the multiplier applied to memory recommendation to calculate limit
                          Default: 1.1 (limit is 10% higher than recommendation)
                          Example: 1.2 means limit = recommendation * 1.2
                        maximum: 10
                        minimum: 1
                        type: number
                    type: object
                  updateRequestsOnly:
                    default: true
                    description: UpdateRequestsOnly controls whether to update only
                      requests or both requests and limits
                    type: boolean
                  useServerSideApply:
                    default: true
                    description: UseServerSideApply enables Server-Side Apply for
                      field-level ownership
                    type: boolean
                type: object
              weight:
                default: 100
                description: |-
                  Weight defines the priority of this policy when multiple policies match the same workload
                  Higher weight policies take precedence. Default weight is 100.
                format: int32
                maximum: 1000
                minimum: 1
                type: integer
            required:
            - metricsConfig
            - mode
            - resourceBounds
            - selector
            - updateStrategy
            type: object
          status:
            description: status defines the observed state of OptimizationPolicy
            properties:
              conditions:
                description: Conditions represent the current state of the OptimizationPolicy
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReconciliation:
                description: LastReconciliation is the timestamp of the last reconciliation
                format: date-time
                type: string
              workloadsByType:
                description: WorkloadsByType provides breakdown of workloads by type
                properties:
                  daemonSets:
                    description: DaemonSets is the count of DaemonSet workloads
                    type: integer
                  deployments:
                    description: Deployments is the count of Deployment workloads
                    type: integer
                  statefulSets:
                    description: StatefulSets is the count of StatefulSet workloads
                    type: integer
                type: object
              workloadsDiscovered:
                description: WorkloadsDiscovered is the count of workloads matching
                  this policy
                type: integer
              workloadsProcessed:
                description: WorkloadsProcessed is the count of workloads successfully
                  processed
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
